import React from 'react';

import ViewProfile from '../../components/profile';

const Profile = () => {
    return (
        <div>
            <ViewProfile />
        </div>
    );
};

export default Profile;
